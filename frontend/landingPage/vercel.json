{
  "installCommand": "npm install --legacy-peer-deps && npm install --prefix ../worker --legacy-peer-deps && npm install --prefix ../manager --legacy-peer-deps",
  "buildCommand": "npm run build",
  "framework": "nextjs"
}
